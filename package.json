{
  "name": "portfolio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "postcss ./src/input.css -o ./dist/output.css --minify",
    "build:blog": "node build.js",
    "build": "npm run build:css && npm run build:blog",
    "build:dev": "cross-env NODE_ENV=development npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "cross-env": "^10.0.0",
    "front-matter": "^4.0.2",
    "marked": "^16.1.2",
    "postcss": "^8.5.3",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.4"
  }
}
